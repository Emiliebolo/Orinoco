console.log(document.getElementById('productBasket'));

// Communication avec l'API avec utilisation de ma fonction ajax qui se trouve dans request Api

class ProductBasket {
    constructor(){
        const ajax = new Ajax();
        ajax.save()
    }
}